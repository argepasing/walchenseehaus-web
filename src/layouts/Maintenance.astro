---

---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="utf-8" />
    <title>WalchenseeHaus | website</title>
    <meta name="description" content="WalchenseeHaus | website" />

    <style>
      :root {
        --oc-white: #fff;
        --oc-black: #000;
        --oc-gray-0: #f8f9fa;
        --oc-gray-1: #f1f3f5;
        --oc-gray-2: #e9ecef;
        --oc-gray-3: #dee2e6;
        --oc-gray-4: #ced4da;
        --oc-gray-5: #adb5bd;
        --oc-gray-6: #868e96;
        --oc-gray-7: #495057;
        --oc-gray-8: #343a40;
        --oc-gray-9: #212529;
      }

      html,
      body {
        height: 100%;
        padding: 0;
        margin: 0;
      }

      body {
        background: radial-gradient(ellipse at center, var(--oc-gray-2), var(--oc-gray-3));
        background-color: var(--oc-gray-3);
      }

      section {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100%;
      }

      main {
        box-sizing: border-box;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        width: 25rem;
        min-height: 15rem;
        padding: 1rem;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif, "Apple Color Emoji",
          "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
        text-align: center;
        background: radial-gradient(ellipse at center, var(--oc-gray-0), var(--oc-gray-2));
        background-color: var(--oc-gray-0);
        border-radius: 0.5rem;
        box-shadow:
          0 20px 25px -5px rgba(0, 0, 0, 0.1),
          0 10px 10px -5px rgba(0, 0, 0, 0.04);
      }

      main h1 {
        font-size: 2.5rem;
        font-weight: 100;
        filter: opacity(0.75);
        transition: 500ms filter linear;
      }

      main:hover h1 {
        filter: opacity(1);
      }

      main h1 .meta {
        color: var(--oc-gray-5);
        font-weight: 100;
      }

      main h1 small {
        display: block;
        margin-top: 2rem;
        font-size: 2.75rem;
        font-weight: 300;
        color: var(--oc-gray-6);
      }
    </style>
  </head>

  <body>
    <section>
      <main>
        <h1>
          WalchenseeHaus<span class="meta">.de</span>
          <small>🏹 🏡 🏔️</small>
        </h1>
      </main>
    </section>

    <script>
      console.log("hello! 👋");
    </script>
  </body>
</html>
